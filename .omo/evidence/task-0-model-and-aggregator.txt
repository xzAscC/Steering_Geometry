# Task 0 Validation: Model & Aggregator

**Date:** 2026-03-13
**Status:** ✅ PASSED

## Validation 1: HookedModel Import

### Test:
```bash
uv run python -c "from steering_geometry.models import HookedModel; ..."
```

### Result:
```
HookedModel import OK
HookedModel is callable: True ✅
```

---

## Validation 2: Discriminative Token Aggregator

### Location:
`src/steering_geometry/extract.py:142`

### Function Signature:
```python
def discriminative_token_aggregator(pos: Tensor, neg: Tensor, top_k: int = 100) -> Tensor:
```

### Test:
```bash
uv run python -c "from steering_geometry.extract import discriminative_token_aggregator; ..."
```

### Result:
```
discriminative_token_aggregator exists: True
Is callable: True ✅
Signature: (pos: torch.Tensor, neg: torch.Tensor, top_k: int = 100) -> torch.Tensor
```

### Additional References:
- Line 190: Used as `partial(discriminative_token_aggregator, top_k=top_k)`
- Line 699: Registered in method list: `"discriminative_token_aggregator"`

## Result:
✅ HookedModel imports and is callable
✅ discriminative_token_aggregator exists and is callable with expected signature
